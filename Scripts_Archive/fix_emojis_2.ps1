$content = Get-Content -Path "js\main.js" -Raw -Encoding UTF8

$content = $content.Replace("?? 图片大小超过1M", "⚠️ 图片大小超过1M")
$content = $content.Replace("?? 图片大小超过2M", "⚠️ 图片大小超过2M")
$content = $content.Replace("?? 头像保存失败", "❌ 头像保存失败")
$content = $content.Replace("?? 存储空间不足", "❌ 存储空间不足")
$content = $content.Replace("?? 数据恢复成功", "✅ 数据恢复成功")
$content = $content.Replace("?? IndexedDB 存储空间", "📊 IndexedDB 存储空间")
$content = $content.Replace("?? 存储空间使用情况", "📊 存储空间使用情况")
$content = $content.Replace("?? 存储降级", "⚠️ 存储降级")
$content = $content.Replace("?? 文件大小超过", "⚠️ 文件大小超过")
$content = $content.Replace("?? 文档内容为空", "⚠️ 文档内容为空")
$content = $content.Replace("?? 不支持的文件格式", "⚠️ 不支持的文件格式")
$content = $content.Replace("?? AI 正在自动提炼", "✨ AI 正在自动提炼")
$content = $content.Replace("?? AI正在回忆", "📸 AI正在回忆")
$content = $content.Replace("?? AI 正在提取", "✨ AI 正在提取")
$content = $content.Replace("?? 正在生成精彩总结", "✨ 正在生成精彩总结")
$content = $content.Replace("?? 离线模式", "📴 离线模式")
$content = $content.Replace("切换为离线模式 ??", "切换为离线模式 📴")
$content = $content.Replace("切换为线上模式 ??", "切换为线上模式 🟢")
$content = $content.Replace("?? 返回主界", "🔙 返回主界")
$content = $content.Replace("听不懂啦??", "听不懂啦🤔")
$content = $content.Replace("?? 我们还是分开吧", "💔 我们还是分开吧")
$content = $content.Replace("?? 正在更新", "🔄 正在更新")
$content = $content.Replace("?? 已解除关系", "💔 已解除关系")
$content = $content.Replace("?? 正在精心准备", "💍 正在精心准备")
$content = $content.Replace("?? 我向你求婚了", "💍 我向你求婚了")
$content = $content.Replace("?? 求婚成功", "🎉 求婚成功")
$content = $content.Replace("?? 背景图保存失败", "❌ 背景图保存失败")
$content = $content.Replace("?? 您的数据可能", "⚠️ 您的数据可能")
$content = $content.Replace("?? 导入备份", "⚠️ 导入备份")
$content = $content.Replace("?? 如果导入", "⚠️ 如果导入")
$content = $content.Replace("?? 这将同时删除", "⚠️ 这将同时删除")
$content = $content.Replace("?? 发布朋友圈失败", "❌ 发布朋友圈失败")
$content = $content.Replace("?? 评论上传一次", "⚠️ 评论上传一次")
$content = $content.Replace("?? 最多只能上传", "⚠️ 最多只能上传")
$content = $content.Replace("?? 回复内容不能", "⚠️ 回复内容不能")
$content = $content.Replace("?? 编辑", "✏️ 编辑")
$content = $content.Replace("?? 版规须知", "📜 版规须知")
$content = $content.Replace("['??', '??', '??', '??', '??', '??', '??', '??']", "['😀', '😂', '😍', '😭', '😡', '😱', '👍', '🙏']")
$content = $content.Replace("emoji: '???'", "emoji: '🍉'")
$content = $content.Replace("emoji: '??'", "emoji: '👻'")
$content = $content.Replace("|| '??'", "|| '💬'")
$content = $content.Replace("<span>??</span> 评论", "<span>💬</span> 评论")
$content = $content.Replace("? ??: ", "❌ 错误: ")
$content = $content.Replace("?? 正在告诉", "💬 正在告诉")
$content = $content.Replace("?? 兼容性处理", "⚠️ 兼容性处理")
$content = $content.Replace("?? 发帖须知", "📜 发帖须知")
$content = $content.Replace("?? 本地上传一次", "⚠️ 本地上传一次")
$content = $content.Replace("?? 封面图保存", "❌ 封面图保存")
$content = $content.Replace("?? 改为“串行”", "⚠️ 改为“串行”")
$content = $content.Replace("?? 点赞", "👍 点赞")
$content = $content.Replace("?? 评论", "💬 评论")
$content = $content.Replace("??? 删除", "🗑️ 删除")
$content = $content.Replace("?? 已赞", "💖 已赞")
$content = $content.Replace("?? 赞", "👍 赞")
$content = $content.Replace("?? 正在生成记忆", "✨ 正在生成记忆")
$content = $content.Replace("?? 心声", "💭 心声")
$content = $content.Replace("?? 地点", "📍 地点")
$content = $content.Replace("?? 心情", "🎭 心情")
$content = $content.Replace("?? 好感度", "❤️ 好感度")
$content = $content.Replace("?? 情欲指数", "🔥 情欲指数")
$content = $content.Replace("来了来了！??", "来了来了！🏃")
$content = $content.Replace("?? 请先在设置中", "⚠️ 请先在设置中")

Set-Content -Path "js\main.js" -Value $content -Encoding UTF8
Write-Host "Fixed js\main.js!"
